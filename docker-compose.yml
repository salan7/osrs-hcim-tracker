services:
  postgres:
    image: postgres:18
    container_name: hcim-postgres
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./backend/schema.sql:/docker-entrypoint-initdb.d/schema.sql

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend-tests:
    build: ./backend
    container_name: hcim-backend-tests
    environment:
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
    command: ["python", "-m", "pytest"]

  frontend-tests:
    build:
      context: ./frontend
      target: build
    container_name: hcim-frontend-tests
    command: ["npm", "test", "--", "--run"]

  backend:
    build: ./backend
    container_name: hcim-backend
    environment:
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
    ports:
      - "5000:5000"
    depends_on:
      postgres:
        condition: service_healthy
      backend-tests:
        condition: service_completed_successfully

  frontend:
    build: ./frontend
    container_name: hcim-frontend
    ports:
      - "5173:80"
    depends_on:
      backend:
        condition: service_started
      frontend-tests:
        condition: service_completed_successfully

volumes:
  postgres_data: